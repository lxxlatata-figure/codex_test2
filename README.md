# 贪吃蛇 (Snake)

一个用 Python + pygame 实现的简单贪吃蛇游戏。

运行说明：

1. 安装依赖：

```bash
pip install -r requirements.txt
```

2. 运行游戏：

```bash
python snake_game.py
```

操作说明：
- 方向键 或 WASD 控制移动
- `P` 暂停/继续
- `R` 或 空格 在游戏结束后重新开始
- `ESC` 退出

分辨率：640x480（网格大小 20px）
